{
  "name": "debug-frontend",
  "version": "1.0.0",
  "description": "An alternative pure JS implementation of npm debug library for browser",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "scripts": {
    "test": "npx serve",
    "build": "npm run build:esm && npm run build:iife",
    "build:esm": "bun build index.js --outfile=dist/index.mjs --target=browser",
    "build:iife": "bun build index.js --outfile=dist/index.js --target=browser --format=iife"
  },
  "author": "<javimosch/notraces> arancibiajav@gmail.com",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "git@github.com:javimosch/debug-frontend.git"
  }
}
